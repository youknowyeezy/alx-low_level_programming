#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: First digit to multiply
 * @y: second num to multiply
 * Return: Result of multiplying x by y
 */

int mul(int x, int y)
{
	return(x * y);
}
